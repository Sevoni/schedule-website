{
  "name": "campus-schedule",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy && wrangler pages deploy frontend --project-name=schedule-worker",
    "tail": "wrangler tail",
    "set-webhook": "node scripts/set-webhook.js"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
